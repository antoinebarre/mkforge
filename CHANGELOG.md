# Changelog

## 0.5.0

- Add structured YAML, heading, chapter, and image contract diagnostics.
- Add stable contract rule identifiers and structured diagnostic targets.
- Add `VerificationReport.has_errors` and `has_warnings`.
- Preserve the 0.4.0 boolean validation API through diagnostic delegation.
